# Plan: Sync Repo and Update README

The goal is to synchronize the git repository state with the current local directory (handling additions, modifications, and deletions) and create a comprehensive English README for this Opencode template.

## 1. Sync Repository State
Execute `git add .` to stage all changes. This will:
- Stage modified files (`AGENTS.md`, `opencode.json`, etc.)
- Stage deleted files (e.g., `instructions/` folder contents)
- Stage untracked files (e.g., `skills/`, `.opencode/`)

This ensures the git index exactly matches the current file system.

## 2. Update README.md
Replace the existing `README.md` with a detailed English guide covering the template's structure and capabilities.

### Draft Content Structure
- **Title**: Opencode Template
- **Overview**: Explanation of the template's purpose (AI agent configuration).
- **Core Components**:
  - **Agents (`agent/`)**: Description of available specialized agents (e.g., `backend-architect`, `frontend-developer`).
  - **Skills (`skills/` & `skill/`)**: Overview of the capability library (e.g., `frontend-design`, `test-driven-development`).
  - **Commands (`command/`)**: List of custom slash commands (e.g., `/brainstorm`, `/commit`).
  - **Superpowers (`superpowers/`)**: Explanation of extended functionality.
  - **Context (`context/`)**: How domain knowledge is organized.
- **Configuration**: Brief note on `opencode.json` and `AGENTS.md`.

## 3. Verification
- Run `git status` to confirm the working tree is clean (everything staged).
- Read the new `README.md` to verify content.
