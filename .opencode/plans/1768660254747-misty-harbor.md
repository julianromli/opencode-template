# Plan: Sync Repository & Update Documentation

I will synchronize the repository to match your local template files and create a comprehensive English README explaining the "Opencode Superpowers Template".

## 1. Synchronize Repository State
- **Goal**: Ensure the git repository exactly matches your current local folder (adding new files, updating modified ones, and removing deleted ones).
- **Command**: `git add -A`
- **Why**: This command stages all changes, including deletions, fulfilling the requirement to "remove if in repo but not local".

## 2. Create README.md
- **Goal**: Document the template's purpose, structure, and features in English.
- **Content Outline**:
    - **Title**: OpenCode Superpowers Template
    - **Overview**: A high-performance template for OpenCode agents with built-in "Superpowers" (skills), strict safety protocols, and MCP integrations.
    - **Key Features**:
        - **Role-Based Agents**: Specialized configurations in `agent/` (e.g., Backend Architect, Frontend Developer).
        - **Superpowers & Skills**: extensive library in `skill/` and `superpowers/` covering TDD, Debugging, UI/UX.
        - **MCP Tooling**: Pre-configured for Firecrawl (Web), Context7 (Docs), and Morph (Edit).
        - **Strict Protocols**: `AGENTS.md` rules for safety (e.g., `.env` handling) and mandatory skill usage.
        - **Web Guidelines**: Comprehensive UI/UX/Accessibility standards.
    - **Directory Structure**: Brief explanation of `agent/`, `context/`, `command/`, `superpowers/`.

## 3. Finalize & Verify
- **Stage README**: `git add README.md`
- **Verify**: Run `git status` to confirm the repository state perfectly mirrors the local filesystem and the new README is included.
- **Commit**: Create a commit to finalize the replacement in the repository history.
    - *Message*: "feat: Replace repo content with Superpowers template and update documentation"

## 4. Verification
- Check `git status` reports a clean working tree (after commit).
- Verify `README.md` content matches the plan.
